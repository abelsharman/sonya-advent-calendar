# 🚀 Инструкция по деплою на сервер

## Вариант 1: Ручной деплой (Быстрый)

### Шаг 1: Собери production билд
```bash
npm run build
```

### Шаг 2: Скопируй файлы на сервер
```bash
# Через scp
scp -r dist/* your_user@your_server_ip:/var/www/advent-calendar/

# Или через rsync (рекомендуется)
rsync -avz --delete dist/ your_user@your_server_ip:/var/www/advent-calendar/
```

### Шаг 3: Настрой права доступа на сервере
```bash
ssh your_user@your_server_ip
sudo chown -R www-data:www-data /var/www/advent-calendar
sudo chmod -R 755 /var/www/advent-calendar
```

### Шаг 4: Настрой Nginx

Скопируй конфигурацию на сервер:
```bash
scp nginx.conf your_user@your_server_ip:/tmp/advent-calendar.conf
```

На сервере:
```bash
# Отредактируй конфиг (замени ВАШ_ДОМЕН.com на свой домен)
sudo nano /tmp/advent-calendar.conf

# Перемести в nginx
sudo mv /tmp/advent-calendar.conf /etc/nginx/sites-available/advent-calendar

# Создай симлинк
sudo ln -s /etc/nginx/sites-available/advent-calendar /etc/nginx/sites-enabled/

# Проверь конфиг
sudo nginx -t

# Перезапусти nginx
sudo systemctl restart nginx
```

### Шаг 5: Установи SSL (Let's Encrypt)

**⚠️ ВАЖНО: PWA работает только через HTTPS!**

```bash
# Установи certbot
sudo apt update
sudo apt install certbot python3-certbot-nginx -y

# Получи сертификат
sudo certbot --nginx -d твой-домен.com -d www.твой-домен.com

# Certbot автоматически настроит nginx для HTTPS
```

После установки SSL раскомментируй HTTPS блок в `nginx.conf` и обнови конфигурацию.

---

## Вариант 2: Автоматический деплой (через скрипт)

### Шаг 1: Настрой deploy.sh
Открой файл `deploy.sh` и замени:
```bash
SERVER_USER="your_user"        # твой пользователь на сервере
SERVER_HOST="your_server_ip"   # IP или домен сервера
SERVER_PATH="/var/www/advent-calendar"
```

### Шаг 2: Дай права на выполнение
```bash
chmod +x deploy.sh
```

### Шаг 3: Запусти деплой
```bash
./deploy.sh
```

Скрипт автоматически:
- Соберёт production билд
- Скопирует файлы на сервер
- Сообщит об успехе/ошибках

---

## Проверка после деплоя

### 1. Проверь что сайт открывается
Открой https://твой-домен.com в браузере

### 2. Проверь PWA
В Chrome DevTools:
- Открой вкладку **Application**
- Проверь **Manifest** - должен быть корректный
- Проверь **Service Workers** - должен быть зарегистрирован

### 3. Проверь на iPhone
1. Открой Safari на iPhone
2. Перейди на https://твой-домен.com
3. Нажми кнопку "Поделиться"
4. Выбери "На экран «Домой»"
5. Приложение должно установиться как PWA

### 4. Проверь уведомления
После настройки Firebase, проверь что работают push-уведомления.

---

## Обновление приложения

Когда нужно обновить приложение:

```bash
# Вариант 1: Автоматический
./deploy.sh

# Вариант 2: Ручной
npm run build
rsync -avz --delete dist/ your_user@your_server_ip:/var/www/advent-calendar/
```

PWA автоматически обновится у пользователей благодаря Service Worker!

---

## Типичные проблемы и решения

### PWA не устанавливается на iPhone
✅ Убедись что используется HTTPS (обязательно!)
✅ Проверь manifest.webmanifest доступен
✅ Проверь иконки загружаются корректно

### Service Worker не регистрируется
✅ Очисти кэш браузера
✅ Проверь что sw.js не кэшируется (см. nginx.conf)
✅ Открой DevTools → Application → Service Workers

### Страница 404 при обновлении
✅ Убедись что в nginx есть `try_files $uri $uri/ /index.html;`

### Статические файлы не обновляются
✅ Vite автоматически добавляет хэши к именам файлов
✅ Очисти кэш браузера (Ctrl+Shift+R)

---

## Мониторинг

Проверяй логи nginx:
```bash
# Логи доступа
sudo tail -f /var/log/nginx/access.log

# Логи ошибок
sudo tail -f /var/log/nginx/error.log
```

---

## 🔒 Безопасность

1. ✅ **Используй только HTTPS**
2. ✅ Настрой файрвол:
   ```bash
   sudo ufw allow 'Nginx Full'
   sudo ufw enable
   ```
3. ✅ Регулярно обновляй сервер:
   ```bash
   sudo apt update && sudo apt upgrade -y
   ```
4. ✅ Автоматическое обновление SSL:
   ```bash
   sudo certbot renew --dry-run
   ```

---

## 🎯 Следующие шаги

После успешного деплоя:
1. ✅ Настрой Firebase для push-уведомлений
2. ✅ Отредактируй подарки в `src/gifts.js`
3. ✅ Протестируй на iPhone Сони
4. ✅ Настрой автоматические уведомления через Firebase Cloud Functions

---

Готово! Теперь Соня сможет установить календарь на свой iPhone! 🎄💝

